configurations:
  - loader: fff
    config:
      - val1: 1
        val2: 3
      - val1: 2
        val2: 4
  - loader: bbb
    config:
      - val1: 1
        val2: 3
      - val1: 2
        val2: 4
-----
HOCON_FILE
  HObjectEntries(OBJECT_ENTRIES)
    HObjectField(OBJECT_FIELD)
      HValuedField(VALUED_FIELD)
        HFieldKey(FIELD_KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('configurations')
        PsiElement(COLON)(':')
        PsiWhiteSpace('\n  ')
        HArray(BLOCK_ARRAY)
          PsiElement(UNQUOTED_CHARS)('-')
          PsiWhiteSpace(' ')
          HObject(BLOCK_OBJECT)
            HObjectEntries(OBJECT_ENTRIES)
              HObjectField(OBJECT_FIELD)
                HValuedField(VALUED_FIELD)
                  HFieldKey(FIELD_KEY)
                    HKeyPart(KEY_PART)
                      HUnquotedString(UNQUOTED_STRING)
                        PsiElement(UNQUOTED_CHARS)('loader')
                  PsiElement(COLON)(':')
                  PsiWhiteSpace(' ')
                  HStringValue(STRING_VALUE)
                    HUnquotedString(UNQUOTED_STRING)
                      PsiElement(UNQUOTED_CHARS)('fff')
              PsiWhiteSpace('\n    ')
              HObjectField(OBJECT_FIELD)
                HValuedField(VALUED_FIELD)
                  HFieldKey(FIELD_KEY)
                    HKeyPart(KEY_PART)
                      HUnquotedString(UNQUOTED_STRING)
                        PsiElement(UNQUOTED_CHARS)('config')
                  PsiElement(COLON)(':')
                  PsiWhiteSpace('\n      ')
                  HArray(BLOCK_ARRAY)
                    PsiElement(UNQUOTED_CHARS)('-')
                    PsiWhiteSpace(' ')
                    HObject(BLOCK_OBJECT)
                      HObjectEntries(OBJECT_ENTRIES)
                        HObjectField(OBJECT_FIELD)
                          HValuedField(VALUED_FIELD)
                            HFieldKey(FIELD_KEY)
                              HKeyPart(KEY_PART)
                                HUnquotedString(UNQUOTED_STRING)
                                  PsiElement(UNQUOTED_CHARS)('val1')
                            PsiElement(COLON)(':')
                            PsiWhiteSpace(' ')
                            HNumber(NUMBER)
                              PsiElement(UNQUOTED_CHARS)('1')
                        PsiWhiteSpace('\n        ')
                        HObjectField(OBJECT_FIELD)
                          HValuedField(VALUED_FIELD)
                            HFieldKey(FIELD_KEY)
                              HKeyPart(KEY_PART)
                                HUnquotedString(UNQUOTED_STRING)
                                  PsiElement(UNQUOTED_CHARS)('val2')
                            PsiElement(COLON)(':')
                            PsiWhiteSpace(' ')
                            HNumber(NUMBER)
                              PsiElement(UNQUOTED_CHARS)('3')
                    PsiWhiteSpace('\n      ')
                    PsiElement(UNQUOTED_CHARS)('-')
                    PsiWhiteSpace(' ')
                    HObject(BLOCK_OBJECT)
                      HObjectEntries(OBJECT_ENTRIES)
                        HObjectField(OBJECT_FIELD)
                          HValuedField(VALUED_FIELD)
                            HFieldKey(FIELD_KEY)
                              HKeyPart(KEY_PART)
                                HUnquotedString(UNQUOTED_STRING)
                                  PsiElement(UNQUOTED_CHARS)('val1')
                            PsiElement(COLON)(':')
                            PsiWhiteSpace(' ')
                            HNumber(NUMBER)
                              PsiElement(UNQUOTED_CHARS)('2')
                        PsiWhiteSpace('\n        ')
                        HObjectField(OBJECT_FIELD)
                          HValuedField(VALUED_FIELD)
                            HFieldKey(FIELD_KEY)
                              HKeyPart(KEY_PART)
                                HUnquotedString(UNQUOTED_STRING)
                                  PsiElement(UNQUOTED_CHARS)('val2')
                            PsiElement(COLON)(':')
                            PsiWhiteSpace(' ')
                            HNumber(NUMBER)
                              PsiElement(UNQUOTED_CHARS)('4')
          PsiWhiteSpace('\n  ')
          PsiElement(UNQUOTED_CHARS)('-')
          PsiWhiteSpace(' ')
          HObject(BLOCK_OBJECT)
            HObjectEntries(OBJECT_ENTRIES)
              HObjectField(OBJECT_FIELD)
                HValuedField(VALUED_FIELD)
                  HFieldKey(FIELD_KEY)
                    HKeyPart(KEY_PART)
                      HUnquotedString(UNQUOTED_STRING)
                        PsiElement(UNQUOTED_CHARS)('loader')
                  PsiElement(COLON)(':')
                  PsiWhiteSpace(' ')
                  HStringValue(STRING_VALUE)
                    HUnquotedString(UNQUOTED_STRING)
                      PsiElement(UNQUOTED_CHARS)('bbb')
              PsiWhiteSpace('\n    ')
              HObjectField(OBJECT_FIELD)
                HValuedField(VALUED_FIELD)
                  HFieldKey(FIELD_KEY)
                    HKeyPart(KEY_PART)
                      HUnquotedString(UNQUOTED_STRING)
                        PsiElement(UNQUOTED_CHARS)('config')
                  PsiElement(COLON)(':')
                  PsiWhiteSpace('\n      ')
                  HArray(BLOCK_ARRAY)
                    PsiElement(UNQUOTED_CHARS)('-')
                    PsiWhiteSpace(' ')
                    HObject(BLOCK_OBJECT)
                      HObjectEntries(OBJECT_ENTRIES)
                        HObjectField(OBJECT_FIELD)
                          HValuedField(VALUED_FIELD)
                            HFieldKey(FIELD_KEY)
                              HKeyPart(KEY_PART)
                                HUnquotedString(UNQUOTED_STRING)
                                  PsiElement(UNQUOTED_CHARS)('val1')
                            PsiElement(COLON)(':')
                            PsiWhiteSpace(' ')
                            HNumber(NUMBER)
                              PsiElement(UNQUOTED_CHARS)('1')
                        PsiWhiteSpace('\n        ')
                        HObjectField(OBJECT_FIELD)
                          HValuedField(VALUED_FIELD)
                            HFieldKey(FIELD_KEY)
                              HKeyPart(KEY_PART)
                                HUnquotedString(UNQUOTED_STRING)
                                  PsiElement(UNQUOTED_CHARS)('val2')
                            PsiElement(COLON)(':')
                            PsiWhiteSpace(' ')
                            HNumber(NUMBER)
                              PsiElement(UNQUOTED_CHARS)('3')
                    PsiWhiteSpace('\n      ')
                    PsiElement(UNQUOTED_CHARS)('-')
                    PsiWhiteSpace(' ')
                    HObject(BLOCK_OBJECT)
                      HObjectEntries(OBJECT_ENTRIES)
                        HObjectField(OBJECT_FIELD)
                          HValuedField(VALUED_FIELD)
                            HFieldKey(FIELD_KEY)
                              HKeyPart(KEY_PART)
                                HUnquotedString(UNQUOTED_STRING)
                                  PsiElement(UNQUOTED_CHARS)('val1')
                            PsiElement(COLON)(':')
                            PsiWhiteSpace(' ')
                            HNumber(NUMBER)
                              PsiElement(UNQUOTED_CHARS)('2')
                        PsiWhiteSpace('\n        ')
                        HObjectField(OBJECT_FIELD)
                          HValuedField(VALUED_FIELD)
                            HFieldKey(FIELD_KEY)
                              HKeyPart(KEY_PART)
                                HUnquotedString(UNQUOTED_STRING)
                                  PsiElement(UNQUOTED_CHARS)('val2')
                            PsiElement(COLON)(':')
                            PsiWhiteSpace(' ')
                            HNumber(NUMBER)
                              PsiElement(UNQUOTED_CHARS)('4')
