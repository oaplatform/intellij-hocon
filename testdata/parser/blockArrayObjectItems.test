my_object_array:
  - field1.a = fg
    field2 = 3
-----
HOCON_FILE
  HObjectEntries(OBJECT_ENTRIES)
    HObjectField(OBJECT_FIELD)
      HValuedField(VALUED_FIELD)
        HFieldKey(FIELD_KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('my_object_array')
        PsiElement(COLON)(':')
        PsiWhiteSpace('\n  ')
        HArray(BLOCK_ARRAY)
          PsiElement(UNQUOTED_CHARS)('-')
          PsiWhiteSpace(' ')
          HObject(BLOCK_OBJECT)
            HObjectEntries(OBJECT_ENTRIES)
              HObjectField(OBJECT_FIELD)
                HPrefixedField(PREFIXED_FIELD)
                  HFieldKey(FIELD_KEY)
                    HKeyPart(KEY_PART)
                      HUnquotedString(UNQUOTED_STRING)
                        PsiElement(UNQUOTED_CHARS)('field1')
                  PsiElement(PERIOD)('.')
                  HValuedField(VALUED_FIELD)
                    HFieldKey(FIELD_KEY)
                      HKeyPart(KEY_PART)
                        HUnquotedString(UNQUOTED_STRING)
                          PsiElement(UNQUOTED_CHARS)('a')
                    PsiWhiteSpace(' ')
                    PsiElement(EQUALS)('=')
                    PsiWhiteSpace(' ')
                    HStringValue(STRING_VALUE)
                      HUnquotedString(UNQUOTED_STRING)
                        PsiElement(UNQUOTED_CHARS)('fg')
              PsiWhiteSpace('\n    ')
              HObjectField(OBJECT_FIELD)
                HValuedField(VALUED_FIELD)
                  HFieldKey(FIELD_KEY)
                    HKeyPart(KEY_PART)
                      HUnquotedString(UNQUOTED_STRING)
                        PsiElement(UNQUOTED_CHARS)('field2')
                  PsiWhiteSpace(' ')
                  PsiElement(EQUALS)('=')
                  PsiWhiteSpace(' ')
                  HNumber(NUMBER)
                    PsiElement(UNQUOTED_CHARS)('3')
