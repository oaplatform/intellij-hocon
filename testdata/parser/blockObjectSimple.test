a:
  v:
    c = 5
    d = 6
-----
HOCON_FILE
  HObjectEntries(OBJECT_ENTRIES)
    HObjectField(OBJECT_FIELD)
      HValuedField(VALUED_FIELD)
        HFieldKey(FIELD_KEY)
          HKeyPart(KEY_PART)
            HUnquotedString(UNQUOTED_STRING)
              PsiElement(UNQUOTED_CHARS)('a')
        PsiElement(COLON)(':')
        PsiWhiteSpace('\n  ')
        HObject(BLOCK_OBJECT)
          HObjectEntries(OBJECT_ENTRIES)
            HObjectField(OBJECT_FIELD)
              HValuedField(VALUED_FIELD)
                HFieldKey(FIELD_KEY)
                  HKeyPart(KEY_PART)
                    HUnquotedString(UNQUOTED_STRING)
                      PsiElement(UNQUOTED_CHARS)('v')
                PsiElement(COLON)(':')
                PsiWhiteSpace('\n    ')
                HObject(BLOCK_OBJECT)
                  HObjectEntries(OBJECT_ENTRIES)
                    HObjectField(OBJECT_FIELD)
                      HValuedField(VALUED_FIELD)
                        HFieldKey(FIELD_KEY)
                          HKeyPart(KEY_PART)
                            HUnquotedString(UNQUOTED_STRING)
                              PsiElement(UNQUOTED_CHARS)('c')
                        PsiWhiteSpace(' ')
                        PsiElement(EQUALS)('=')
                        PsiWhiteSpace(' ')
                        HNumber(NUMBER)
                          PsiElement(UNQUOTED_CHARS)('5')
                    PsiWhiteSpace('\n    ')
                    HObjectField(OBJECT_FIELD)
                      HValuedField(VALUED_FIELD)
                        HFieldKey(FIELD_KEY)
                          HKeyPart(KEY_PART)
                            HUnquotedString(UNQUOTED_STRING)
                              PsiElement(UNQUOTED_CHARS)('d')
                        PsiWhiteSpace(' ')
                        PsiElement(EQUALS)('=')
                        PsiWhiteSpace(' ')
                        HNumber(NUMBER)
                          PsiElement(UNQUOTED_CHARS)('6')
